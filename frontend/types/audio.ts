// Audio engine related types

// (Currently audioEngine.ts doesn't have many type declarations, but separated for future extensibility)

export type PlayheadUpdateCallback = (flicks: number) => void;
